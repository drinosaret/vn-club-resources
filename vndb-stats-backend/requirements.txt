# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18

# Database
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
psycopg[binary]>=3.2.0

# Data processing (unpinned for Python 3.14 compatibility)
pandas>=2.2.0
numpy>=2.0.0
scipy>=1.14.0

# ML/Recommendations (unpinned for Python 3.14 compatibility)
scikit-learn>=1.5.0
implicit>=0.7.0

# Collaborative filtering is trained with implicit (ALS); scikit-learn serves the
# similarity arithmetic in the recommender.

# RecBole for recommendation benchmarking (optional - install separately for GPU support)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# pip install recbole

# VNDB API client
httpx>=0.28.0
aiohttp>=3.11.0

# Caching
redis>=5.2.0

# Task scheduling
apscheduler>=3.10.0

# News aggregation
feedparser>=6.0.0
defusedxml>=0.7.0 
beautifulsoup4>=4.12.0
lxml>=5.3.0
python-dateutil>=2.9.0

# File handling (unpinned for Python 3.14)
zstandard>=0.23.0
orjson>=3.10.0

# Validation
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Discord Bot
discord.py>=2.3.0

# Image rendering
Pillow>=11.0.0

# Rate limiting
slowapi>=0.1.9

# Process monitoring
psutil>=5.9.0

# Description embeddings, nightly worker job only. Inference runs on the ONNX runtime so
# no deep learning framework is installed: these three add roughly 75 MB, where a torch
# based stack adds well over a gigabyte. Serving reads a prebuilt matrix with numpy and
# imports none of them.
onnxruntime>=1.20.0
tokenizers>=0.20.0
huggingface-hub>=0.26.0
